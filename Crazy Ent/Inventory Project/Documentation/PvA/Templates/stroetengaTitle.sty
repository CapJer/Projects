\ProvidesPackage{/templates/stroetengaTitle}
\RequirePackage{graphicx} % make sure graphicx is included as a usepackage in header
\RequirePackage{tikz} % make sure tikz is included as a usepackage in header
\RequirePackage{textpos}

% define the value by calling \variable{companyname} , then print it with \@variable
\newcommand*{\company}[1]{\gdef\@company{#1}} % het bedrijf
\newcommand*{\@company}{Stroetenga Technologies} % define a default value

\newcommand*{\supervisor}[1]{\gdef\@supervisor{#1}} % stagebegeleider
\newcommand*{\@supervisor}{Ludo Stroetenga} % define a default value

\newcommand*{\doctype}[1]{\gdef\@doctype{#1}} % wat voor document
\newcommand*{\@doctype}{Documentatie} % define a default value

\newcommand*{\studentno}[1]{\gdef\@studentno{#1}} % studentnummer
\newcommand*{\@studentno}{80209} % define a default value

\newcommand*{\studentedu}[1]{\gdef\@studentedu{#1}} % opleiding
\newcommand*{\@studentedu}{Applicatie- / mediaontwikkeling} % define a default value

\newcommand*{\internship}[1]{\gdef\@internship{#1}} % stageperiode
\newcommand*{\@internship}{01-01-2018 tot 01-01-2019} % define a default value

\renewcommand*{\maketitle}{ %
\begin{titlepage}
	 \begin{tikzpicture}[remember picture,overlay]
    		\node[anchor=north west, yshift=17.6ex, xshift=-0.8ex]%
       		 at (current page.north west)
        			{\includegraphics[width=\paperwidth]{images/header.png}};
    	\end{tikzpicture}
	\begin{tikzpicture}[remember picture,overlay]
    		\node[anchor=north west, yshift=10.03ex, xshift=6ex]%
       		 at (current page.north west)
        			{\includegraphics[width=6cm]{images/logo.png}}; 
        	\end{tikzpicture}
	\begin{textblock*}{10cm}(4.5cm, 2.2cm)
		{\scshape\huge \@company \par}
	\end{textblock*}
	\centering
		\vspace{7.5cm}
		{\scshape\Large \@doctype \par}
		\vspace{0.5cm}
		{\huge\bfseries \@title \par}
		\vspace{1cm}
		{\Large\itshape \@author \par}
		\vfill
		{\par}
		{\large \@supervisor}
		\vfill
		{\large \@internship \par}
\end{titlepage}
}
\endinput